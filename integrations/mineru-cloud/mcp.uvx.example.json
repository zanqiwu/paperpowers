{
  "mcpServers": {
    "mineru-cloud": {
      "command": "uvx",
      "args": [
        "mcp-server-mineru-bach"
      ],
      "env": {
        "MINERU_TOKEN": "${MINERU_TOKEN}",
        "MINERU_BASE_URL": "${MINERU_BASE_URL}"
      }
    }
  }
}
